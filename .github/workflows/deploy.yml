name: Deploy to VPS

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test

      - name: Build
        run: npm run web:build
        env:
          SUPABASE_URL: ${{ vars.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ vars.SUPABASE_ANON_KEY }}

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.VPS_SSH_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H ${{ vars.VPS_HOST }} >> ~/.ssh/known_hosts

      - name: Deploy dist
        run: |
          rsync -avz --delete \
            -e "ssh -i ~/.ssh/deploy_key" \
            dist/ ${{ vars.VPS_USER }}@${{ vars.VPS_HOST }}:/var/www/master/dist/

      - name: Deploy migrations
        run: |
          rsync -avz \
            -e "ssh -i ~/.ssh/deploy_key" \
            supabase/migrations/ ${{ vars.VPS_USER }}@${{ vars.VPS_HOST }}:/opt/master/supabase/migrations/

      - name: Deploy Edge Functions
        run: |
          rsync -avz \
            -e "ssh -i ~/.ssh/deploy_key" \
            supabase/functions/ ${{ vars.VPS_USER }}@${{ vars.VPS_HOST }}:/opt/master/supabase/functions/

      - name: Deploy docker-compose
        run: |
          ssh -i ~/.ssh/deploy_key \
            ${{ vars.VPS_USER }}@${{ vars.VPS_HOST }} \
            "mkdir -p /opt/master/supabase/docker"
          rsync -avz \
            -e "ssh -i ~/.ssh/deploy_key" \
            supabase/docker/docker-compose.yml ${{ vars.VPS_USER }}@${{ vars.VPS_HOST }}:/opt/master/supabase/docker/docker-compose.yml

      - name: Deploy scripts
        run: |
          rsync -avz \
            -e "ssh -i ~/.ssh/deploy_key" \
            deploy/ ${{ vars.VPS_USER }}@${{ vars.VPS_HOST }}:/opt/master/deploy/

      - name: Deploy nginx config
        run: |
          rsync -avz \
            -e "ssh -i ~/.ssh/deploy_key" \
            deploy/nginx.conf ${{ vars.VPS_USER }}@${{ vars.VPS_HOST }}:/etc/nginx/sites-available/master

      - name: Run migrations
        run: |
          ssh -i ~/.ssh/deploy_key \
            ${{ vars.VPS_USER }}@${{ vars.VPS_HOST }} \
            "bash /opt/master/deploy/migrate.sh"

      - name: Rebuild and restart Edge Functions
        run: |
          ssh -i ~/.ssh/deploy_key \
            ${{ vars.VPS_USER }}@${{ vars.VPS_HOST }} \
            "cd /opt/master/supabase/docker && docker compose build functions && docker compose up -d functions && nginx -t && systemctl reload nginx"
